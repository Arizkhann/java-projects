-- curdate(): used to display current date
select curdate();
-- curtime(): used to display current time 
select curTime();
-- dayofweek(): display day of week
select dayofweek(curdate());

select dayofmonth(curdate());

select quarter(curdate());